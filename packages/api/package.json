{
  "name": "@donor-match/api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --hot src/server.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "typecheck": "tsc --noEmit",
    "build": "bun build ./src/server.ts --outdir ./dist && tsc"
  },
  "dependencies": {
    "@clerk/backend": "*",
    "@donor-match/db": "workspace:*",
    "@donor-match/ts-rest": "workspace:*",
    "@fastify/cookie": "*",
    "@fastify/jwt": "*",
    "@fastify/request-context": "*",
    "@fastify/swagger": "*",
    "@fastify/swagger-ui": "*",
    "@prisma/client": "*",
    "@ts-rest/fastify": "*",
    "@ts-rest/open-api": "*",
    "@zenstackhq/runtime": "*",
    "fastify": "*",
    "fastify-plugin": "*"
  },
  "devDependencies": {
    "pino-pretty": "*",
    "zenstack": "*"
  }
}
