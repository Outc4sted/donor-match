{
  "name": "@repo/db",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "exports": {
    "./prisma/*": {
      "types": "./dist/prisma/generated/prisma-client/*.d.ts",
      "default": "./dist/prisma/generated/prisma-client/*.js"
    },
    "./prisma/errors": {
      "types": "./dist/errors/index.d.ts",
      "default": "./dist/errors/index.js"
    },
    "./zenstack/enhance": {
      "types": "./dist/prisma/generated/enhance.d.ts",
      "default": "./dist/prisma/generated/enhance.js"
    },
    "./types": {
      "types": "./dist/types.d.ts",
      "default": "./dist/types.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "check-types": "tsc --noEmit --incremental",
    "dev": "concurrently -n models,ts -c blue,green \"pnpm run watch:models\" \"pnpm run watch:ts\"",
    "env:docker": "dmno resolve --service @repo/db --format env > .dmno/.env.docker",
    "lint": "eslint --fix --cache --max-warnings 0",
    "schema:gen": "zenstack generate --schema ./src/schema.zmodel -o ./src/prisma/generated",
    "schema:push": "dmno run -- prisma db push --schema=./src/prisma/schema.prisma",
    "seed": "dmno run -- prisma db seed",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch:models": "nodemon --watch src/models --ext zmodel --exec \"pnpm run schema:gen && pnpm run schema:push\" --on-change-only",
    "watch:ts": "tsc -w -p tsconfig.build.json"
  },
  "lint-staged": {
    "**/*.{ts,tsx}": [
      "sh -c 'pnpm --filter=@repo/db run check-types'",
      "pnpm --filter=@repo/db run lint"
    ],
    "**/*.{mjs,ts,tsx,json,md,mdx}": [
      "pnpm -w run prettier"
    ],
    "**/*.{md,mdx}": [
      "pnpm -w run lint:md"
    ]
  },
  "prettier": "@repo/prettier",
  "dependencies": {
    "@prisma/client": "*",
    "@zenstackhq/runtime": "*",
    "dmno": "catalog:dmno"
  },
  "devDependencies": {
    "@faker-js/faker": "*",
    "@repo/eslint": "workspace:*",
    "@repo/prettier": "workspace:*",
    "@repo/tsconfig": "workspace:*",
    "@types/node": "*",
    "concurrently": "*",
    "eslint": "catalog:",
    "nodemon": "*",
    "prettier": "catalog:",
    "prisma": "*",
    "tsx": "*",
    "typescript": "catalog:",
    "zenstack": "*"
  },
  "prisma": {
    "seed": "tsx src/seeds/index.ts"
  }
}
