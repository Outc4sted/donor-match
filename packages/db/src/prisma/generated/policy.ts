/******************************************************************************
 * This file was generated by ZenStack CLI.
 ******************************************************************************/

/* eslint-disable */
// @ts-nocheck

import { type QueryContext, type CrudContract, type PermissionCheckerContext } from "@zenstackhq/runtime";
import { allFieldsEqual } from "@zenstackhq/runtime/validation";
import { type PolicyDef, type PermissionCheckerConstraint } from "@zenstackhq/runtime/enhancements/node";
import { OrganType } from "./prisma-client-js";
import { BloodType } from "./prisma-client-js";

const policy: PolicyDef = {
    policy: {
        patients: {
            modelLevel: {
                read: { guard: patients_read, },
                create: { guard: patients_create, inputChecker: patients_create_input, },
                update: { guard: patients_update, },
                postUpdate: { guard: patients_postUpdate, },
                delete: { guard: patients_delete, }
            },
            fieldLevel: {
                read:
                {

                },
                update:
                {

                },
            },

        },
        organs: {
            modelLevel: {
                read: { guard: organs_read, },
                create: { guard: organs_create, inputChecker: organs_create_input, },
                update: { guard: organs_update, },
                postUpdate: { guard: organs_postUpdate, },
                delete: { guard: organs_delete, }
            },
            fieldLevel: {
                read:
                {

                },
                update:
                {

                },
            },

        },
        waitlist: {
            modelLevel: {
                read: { guard: waitlist_read, },
                create: { guard: waitlist_create, inputChecker: waitlist_create_input, },
                update: { guard: waitlist_update, },
                postUpdate: { guard: waitlist_postUpdate, },
                delete: { guard: waitlist_delete, }
            },
            fieldLevel: {
                read:
                {

                },
                update:
                {

                },
            },

        },
    },
    validation: {
        patients: { hasValidation: false },
        organs: { hasValidation: false },
        waitlist: { hasValidation: false },
    },
    authSelector: { "role": true },
};

function patients_read(context: QueryContext, db: CrudContract): any {
    const user: any = context.user ?? null;
    if (((user?.role == 'org:admin') || (user?.role == 'admin'))) { return { AND: [] }; } if ((user != null)) { return { AND: [] }; } return { OR: [] };
}

function $check_patients_read(input: any, context: QueryContext): any {
    const user: any = context.user ?? null;
    if (((user?.role == 'org:admin') || (user?.role == 'admin'))) { return true; }

    if ((user != null)) { return true; }

    return false;
}

function patients_create(context: QueryContext, db: CrudContract): any {
    const user: any = context.user ?? null;
    if (((user?.role == 'org:admin') || (user?.role == 'admin'))) { return { AND: [] }; } return { OR: [] };
}

function $check_patients_create(input: any, context: QueryContext): any {
    const user: any = context.user ?? null;
    if (((user?.role == 'org:admin') || (user?.role == 'admin'))) { return true; }

    return false;
}

function patients_create_input(input: any, context: QueryContext): boolean {
    const user: any = context.user ?? null;
    return ((user?.role == 'org:admin') || (user?.role == 'admin'))
}

function patients_update(context: QueryContext, db: CrudContract): any {
    const user: any = context.user ?? null;
    if (((user?.role == 'org:admin') || (user?.role == 'admin'))) { return { AND: [] }; } return { OR: [] };
}

function $check_patients_update(input: any, context: QueryContext): any {
    const user: any = context.user ?? null;
    if (((user?.role == 'org:admin') || (user?.role == 'admin'))) { return true; }

    return false;
}

function patients_postUpdate(context: QueryContext, db: CrudContract): any {
    return { AND: [] };
}

function $check_patients_postUpdate(input: any, context: QueryContext): any {
    return true;
}

function patients_delete(context: QueryContext, db: CrudContract): any {
    const user: any = context.user ?? null;
    if (((user?.role == 'org:admin') || (user?.role == 'admin'))) { return { AND: [] }; } return { OR: [] };
}

function $check_patients_delete(input: any, context: QueryContext): any {
    const user: any = context.user ?? null;
    if (((user?.role == 'org:admin') || (user?.role == 'admin'))) { return true; }

    return false;
}

function organs_read(context: QueryContext, db: CrudContract): any {
    const user: any = context.user ?? null;
    if (((user?.role == 'org:admin') || (user?.role == 'admin'))) { return { AND: [] }; } if ((user != null)) { return { AND: [] }; } return { OR: [] };
}

function $check_organs_read(input: any, context: QueryContext): any {
    const user: any = context.user ?? null;
    if (((user?.role == 'org:admin') || (user?.role == 'admin'))) { return true; }

    if ((user != null)) { return true; }

    return false;
}

function organs_create(context: QueryContext, db: CrudContract): any {
    const user: any = context.user ?? null;
    if (((user?.role == 'org:admin') || (user?.role == 'admin'))) { return { AND: [] }; } return { OR: [] };
}

function $check_organs_create(input: any, context: QueryContext): any {
    const user: any = context.user ?? null;
    if (((user?.role == 'org:admin') || (user?.role == 'admin'))) { return true; }

    return false;
}

function organs_create_input(input: any, context: QueryContext): boolean {
    const user: any = context.user ?? null;
    return ((user?.role == 'org:admin') || (user?.role == 'admin'))
}

function organs_update(context: QueryContext, db: CrudContract): any {
    const user: any = context.user ?? null;
    if (((user?.role == 'org:admin') || (user?.role == 'admin'))) { return { AND: [] }; } return { OR: [] };
}

function $check_organs_update(input: any, context: QueryContext): any {
    const user: any = context.user ?? null;
    if (((user?.role == 'org:admin') || (user?.role == 'admin'))) { return true; }

    return false;
}

function organs_postUpdate(context: QueryContext, db: CrudContract): any {
    return { AND: [] };
}

function $check_organs_postUpdate(input: any, context: QueryContext): any {
    return true;
}

function organs_delete(context: QueryContext, db: CrudContract): any {
    const user: any = context.user ?? null;
    if (((user?.role == 'org:admin') || (user?.role == 'admin'))) { return { AND: [] }; } return { OR: [] };
}

function $check_organs_delete(input: any, context: QueryContext): any {
    const user: any = context.user ?? null;
    if (((user?.role == 'org:admin') || (user?.role == 'admin'))) { return true; }

    return false;
}

function waitlist_read(context: QueryContext, db: CrudContract): any {
    const user: any = context.user ?? null;
    if (((user?.role == 'org:admin') || (user?.role == 'admin'))) { return { AND: [] }; } if ((user != null)) { return { AND: [] }; } return { OR: [] };
}

function $check_waitlist_read(input: any, context: QueryContext): any {
    const user: any = context.user ?? null;
    if (((user?.role == 'org:admin') || (user?.role == 'admin'))) { return true; }

    if ((user != null)) { return true; }

    return false;
}

function waitlist_create(context: QueryContext, db: CrudContract): any {
    const user: any = context.user ?? null;
    if (((user?.role == 'org:admin') || (user?.role == 'admin'))) { return { AND: [] }; } return { OR: [] };
}

function $check_waitlist_create(input: any, context: QueryContext): any {
    const user: any = context.user ?? null;
    if (((user?.role == 'org:admin') || (user?.role == 'admin'))) { return true; }

    return false;
}

function waitlist_create_input(input: any, context: QueryContext): boolean {
    const user: any = context.user ?? null;
    return ((user?.role == 'org:admin') || (user?.role == 'admin'))
}

function waitlist_update(context: QueryContext, db: CrudContract): any {
    const user: any = context.user ?? null;
    if (((user?.role == 'org:admin') || (user?.role == 'admin'))) { return { AND: [] }; } return { OR: [] };
}

function $check_waitlist_update(input: any, context: QueryContext): any {
    const user: any = context.user ?? null;
    if (((user?.role == 'org:admin') || (user?.role == 'admin'))) { return true; }

    return false;
}

function waitlist_postUpdate(context: QueryContext, db: CrudContract): any {
    return { AND: [] };
}

function $check_waitlist_postUpdate(input: any, context: QueryContext): any {
    return true;
}

function waitlist_delete(context: QueryContext, db: CrudContract): any {
    const user: any = context.user ?? null;
    if (((user?.role == 'org:admin') || (user?.role == 'admin'))) { return { AND: [] }; } return { OR: [] };
}

function $check_waitlist_delete(input: any, context: QueryContext): any {
    const user: any = context.user ?? null;
    if (((user?.role == 'org:admin') || (user?.role == 'admin'))) { return true; }

    return false;
}
export default policy
